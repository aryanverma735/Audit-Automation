WITH BASE AS(
SELECT * FROM PROV 
WHERE NPI = '1922726801'
AND PROV.MSTR_PROV_ID = '1101618575'
)
SELECT NPI,
CASE WHEN COUNT(*) > 0 THEN 'AUDIT PASS' ELSE 'AUDIT FAIL' END AS AUDIT_RESULT
FROM BASE 
GROUP BY NPI
